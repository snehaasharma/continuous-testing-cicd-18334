# Lab 2 — Submission template

**Name:** ______________________ **Date:** ______________________

## 1. CLI log snippet

Paste the tail of a successful **`./mvnw test`** (or `mvn test`) showing `Tests run:` and `BUILD SUCCESS`:

```
(paste here)
```

Paste the tail of **`./mvnw test -Dgroups=smoke`** showing fewer tests executed:

```
(paste here)
```

## 2. Exit code after a deliberate failure

After breaking a test on purpose, what was **`echo $?`** (Unix/macOS) or `%ERRORLEVEL%` (Windows)?

**Answer:** ______________________

## 3. Report screenshot

Attach or embed a screenshot of the Surefire HTML report (**`target/reports/surefire.html`**) in your browser.

*(Or note the path you opened if submitting via LMS that accepts files separately.)*

## 4. One-line reflection

What is one change you would make so this project is **more CI-friendly** for your team?

-
